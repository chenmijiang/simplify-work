/**
 * 1. 提示：选择仓库类型 npm / yarn
 * 2. 选择仓库类型后，展示当前仓库源地址
 * 3. 提示：切换到哪个源地址
 */
import SW from "@/types";

const main: SW.ExecBashFunction<SW.ChangeSourceConfig> = async (config) => {};

export default main;
