/**
 * 1. 检测是否有未提交的文件，没有提示添加到暂存区
 * 2. 执行 cz-customizable 提交
 */
import SW from "@/types";

const main: SW.ExecBashFunction = async () => {
  console.info("todo implement git-deploy");
};

export default main;
